export interface CreateBookingRequest {
  userId: string;
  // amenitySlug: string;
  bookingDate: string;
  // unit: string;
  unitId: number;
  slotId: number;
  // time: string;
  amenityId: number;
  // startTime: string;
  // endTime: string;
}